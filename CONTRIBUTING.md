# CONTRIBUTING.md

## 概要

このリポジトリの開発フロー、コミット方針、レビュー基準を定義する。

公開アーキテクチャとプロダクト制約は `AGENTS.md` および `docs/` 配下の文書に従うこと。
実装前に以下を確認する。

- `AGENTS.md`
- `README.md`
- `docs/PRODUCT_REQUIREMENTS.md`
- `docs/ARCHITECTURE.md`
- 変更対象に対応する `docs/*`

---

## ブランチ戦略

- 開発フローは GitHub Flow を採用する
- `main` は常にデプロイ可能な状態を保つ
- 作業は `main` から短命ブランチを切って進める
- 作業完了後は Pull Request を作成し、レビュー後に `main` へマージする

### ブランチ命名規則

以下を標準とする。

- `feat/...`: 機能追加
- `fix/...`: バグ修正
- `docs/...`: ドキュメント更新
- `chore/...`: 雑多な保守作業
- `infra/...`: CI/CD やインフラ変更

例:

- `feat/astro-site-shell`
- `fix/sanity-post-query`
- `docs/update-deployment-guide`

---

## コミット方針

- 1 コミット 1 意図を原則とする
- レビューしやすい単位で分ける
- リファクタ、整形、機能変更を可能な限り分離する
- 仕様変更を伴う実装は、関連ドキュメント更新を同じ PR に含める

避けること:

- 無関係な変更を 1 コミットに混ぜる
- 大量の整形変更とロジック変更を同時に入れる
- ドキュメントだけ古いまま実装を進める

---

## Pull Request 方針

- 1 PR 1 目的を原則とする
- スキーマ、画面、インフラの変更を無制限に混在させない
- 変更が広い場合は、依存順に分割して PR を作る
- Draft PR を活用して途中段階でもレビュー可能にする

### PR に必ず含めること

- 背景
- 変更内容
- 確認方法
- 影響範囲
- 未解決事項または後続タスク

### マージ条件

- CI が成功していること
- 最低 1 名の承認があること
- unresolved comment が残っていないこと
- 必要なドキュメント更新が含まれていること

### マージ方式

- 標準は `merge commit`
- PR は必ずマージコミットを作成して統合する
- マージ後の履歴は、PR 単位で追跡しやすい状態を保つ

---

## レビュー観点

レビューでは少なくとも以下を確認する。

- `AGENTS.md` と各種 docs の制約に反していないか
- Astro + Sanity + S3 + CloudFront の固定アーキテクチャを崩していないか
- draft が公開サイトに出ない実装になっているか
- SEO 要件を壊していないか
- シークレットやトークンが公開物に含まれないか
- 不要な依存追加や過剰な抽象化がないか
- ドキュメント更新漏れがないか
- 静的配信前提で 404 や canonical URL の挙動が破綻していないか

---

## ドキュメント更新ルール

変更時は以下を基準に更新する。

- 要件変更: `docs/PRODUCT_REQUIREMENTS.md`
- 構成や責務変更: `docs/ARCHITECTURE.md`
- スキーマ変更: `docs/CONTENT_MODEL.md`
- デプロイ手順変更: `docs/DEPLOYMENT.md`
- 運用手順変更: `docs/OPERATIONS.md`
- リリース確認項目変更: `docs/RELEASE_CHECKLIST.md`
- 恒久的な設計判断: `docs/DECISIONS.md`

`README.md` は入口文書として保ち、詳細手順は個別文書へ寄せる。

---

## 実装開始前チェック

作業前に以下を確認する。

- 変更の目的が 1 つに絞れているか
- どの文書を更新すべきか判断できているか
- 既存仕様と矛盾がないか
- 変更が `main` にマージされたときにデプロイ可能か

---

## リリース前の開発者チェック

PR を Ready for Review にする前に、少なくとも以下を確認する。

- ローカル build が成功する
- 主要ページまたは主要機能の動作を確認した
- 新規環境変数がある場合は `env.example` と関連 docs を更新した
- 公開ページに秘密情報が含まれない
- 必要な docs 更新を完了した

# 2026-08-22 Progress

## 概要

Build Ledger UIのホーム見出しを、Sanity StudioのSite settingsから編集できる任意の`siteTagline`として実装した。既存のSite settingsに値がない場合は、現在の文言をフォールバック表示する。

## ブランチ

- ブランチ: `feat/dark-ui-redesign`
- 設計仕様: `docs/superpowers/specs/2026-08-22-site-tagline-design.md`
- 仕様コミット: `bc8a6a1`

## 完了した項目

### 1. Sanityスキーマ

- Site settingsに任意の`siteTagline`文字列フィールドを追加
- Studio上のタイトルは`Site tagline`
- 最大80文字のvalidationを追加
- 未入力時に既定文言を使用することをdescriptionへ明記

### 2. 公開サイト

- Site settings GROQクエリで`siteTagline`を取得
- `SiteSettings`型へ`siteTagline`を追加
- 環境変数やSanityクライアントに依存しない正規化モジュールを追加
- 設定値の前後空白を除去し、未定義・空文字・空白だけの場合は既定文言へフォールバック
- ホームの大見出しをハードコードから`siteSettings.siteTagline`参照へ変更
- SEOメタデータとRSSにはサブタイトルを転用しない

### 3. 文書

- `docs/CONTENT_MODEL.md`へ`siteTagline`の用途、任意入力、最大文字数、フォールバックを追記

## 検証結果

- `pnpm format:check`: 成功
- `pnpm lint`: 成功
- `pnpm build`: 成功
- Astro check: 39ファイル、エラー・警告・ヒントなし
- `pnpm sanity:build`: 成功
- 設定値、未定義、空文字、空白だけの4ケースを直接検証
- 生成HTMLで既定サブタイトル、既存title、meta descriptionを確認
- RSSにサブタイトルが転用されていないことを確認

## リリース後の確認

1. `main`へのマージ後、`Deploy Studio`が成功することを確認する
2. StudioのSite settingsに`Site tagline`が表示されることを確認する
3. 任意の文言を入力してPublishし、Sanity webhookから`Deploy Site`が起動することを確認する
4. CloudFront経由のホーム画面に入力値が反映されることを確認する
5. `Site tagline`を空にしてPublishし、既定文言へ戻ることを確認する

# 2026-08-09 Progress

## 概要

2026-05-24 時点で残っていた Sanity Studio の本番デプロイ確認を完了した。
GitHub Actions の `Deploy Studio` は全工程が成功し、CloudFront の配信先へ外部からアクセスできることも確認済み。

加えて、ChatGPTの公開共有リンクを日本語の技術記事へ再構成し、安全にSanity Draftを作成するリポジトリローカルSkillと再利用ツールを導入した。PR #12は `main` へマージ済み。

---

## 現在のブランチ状態

- ブランチ: `feat/chatgpt-share-to-sanity-draft`
- リモート: `origin/feat/chatgpt-share-to-sanity-draft` を追跡
- PR: [#12 Add ChatGPT share-to-Sanity draft workflow](https://github.com/dodo5522/blog-tech/pull/12) は `main` へマージ済み
- merge commit: `fdaf1ca963d2670ab3b2d0a6d68633056d06340d`
- 作業ツリー: 本記録の変更前は clean

---

## 完了した項目

### 1. GitHub Repository Variables の設定確認

`Deploy Studio` workflow が Terraform backend を参照するための以下の変数について、実際の workflow が成功する設定になっていることを確認した。

- `TF_STATE_BUCKET_NAME`
- `TF_STATE_KEY`
- `TF_STATE_REGION`

### 2. デプロイ用 IAM 権限の確認

- `AWS_DEPLOY_ROLE_ARN` の IAM ロールで Terraform state を参照できることを実際の workflow 実行で確認
- Terraform backend 初期化と output 取得が成功
- state lock を含む現在の backend 構成で権限不足が発生しないことを確認

### 3. Deploy Studio workflow の本番確認

GitHub Actions の実行ログで、以下の全工程が成功していることを確認した。

- Terraform backend の初期化
- Terraform output から Studio 用 S3 bucket / CloudFront distribution ID を取得
- Sanity Studio の build
- S3 sync
- CloudFront invalidation

### 4. CloudFront 配信確認

- 配信先: <https://d2p25iraet5nnt.cloudfront.net>
- ブラウザからアクセスできることを確認
- 外部からの HTTP 確認で `HTTP/2 200` を確認
- S3 上の HTML が CloudFront 経由で配信されていることをレスポンスヘッダーで確認

### 5. リポジトリローカルSkillの導入

- `gonta223/humanizer-ja` を `.agents/skills/humanizer-ja` へ導入
- `.agents/skills/chatgpt-share-to-sanity-draft` を作成
- 公開共有リンク、会話エクスポート、貼り付け本文を入力として扱える運用を整理
- ユーザーの実測、ChatGPTの回答、現在の一次資料を区別する編集基準を追加
- APIトークン、Cookie、個人情報、共有会話をGitへ含めないルールを明文化

### 6. 再利用ツールの追加

- `extract-chatgpt-share.mjs`: ChatGPT公開共有ページからuser/assistant発言を抽出
- `check-sanity-draft.mjs`: 同一slugのSanity Draft・公開済み記事を投稿前に検出
- 抽出物と記事パッケージはGit管理外の `tmp/` またはOSの `/tmp` に限定
- Sanityの認証情報はローカル環境変数またはGit管理外の `.env` 系ファイルだけで管理

### 7. 実運用と安全性の確認

- サンプル共有URLから96メッセージを抽出し、記事を作成
- `humanizer-ja` で推敲した記事をSanity Draftとして投入し、人による軽微な手直しで利用可能な品質であることを確認
- 同一slugの再importがStudio上の編集を上書きし得るため、投稿前チェックを必須化
- レビュー指摘を受け、既存記事チェックを固定 `_id` ではなく `_type == "post" && slug.current == $slug` の検索へ修正
- Sanity実接続で、既存slugは終了コード3、存在しないslugは終了コード0となることを確認

### 8. PRとCI

- PR #12を作成し、レビュー指摘1件へ対応
- Prettier対象だったリポジトリローカルSkillのMarkdownを整形
- GitHub Actionsの `validate` が成功
- PR #12は2026-08-09に `main` へマージ済み

---

## 残件

2026-05-24 の steering に記載されていた残件はすべて完了。

ChatGPT共有リンクからSanity Draftを作成する今回のタスクにも残件はない。レビューコメントへの返信・スレッド解決はコード変更とは別のGitHub操作であり、マージ時点で追加対応は不要。

次の作業開始時は、リリース全体の Definition of Done と過去の steering を照合し、サイト本体の本番デプロイ、Sanity webhook、CDN の 404 挙動などに未確認項目が残っていないか再監査する。

---

## 次に着手すべき順序

1. 過去の steering と `docs/RELEASE_CHECKLIST.md` を照合して、未完了項目を再整理する
2. 未確認項目があれば、本番環境で end-to-end 検証する
3. 検証結果を運用ドキュメントと次回 steering に反映する
